{
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "description": "A URL to the JSON Schema for this object.",
      "examples": [
        "https://example.com/schemas/CreateStripeProductRequest.json"
      ],
      "format": "uri",
      "readOnly": true,
      "type": "string"
    },
    "description": {
      "examples": ["Get premium content with this subscription"],
      "type": "string"
    },
    "name": { "examples": ["Premium Plan"], "type": "string" }
  },
  "required": ["name", "description"],
  "type": "object",
  "x-readme-ref-name": "CreateStripeProductRequest"
}
