{
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "description": "A URL to the JSON Schema for this object.",
      "examples": ["https://example.com/schemas/CreateUserResponse.json"],
      "format": "uri",
      "readOnly": true,
      "type": "string"
    },
    "id": {
      "description": "ID of the user",
      "examples": ["1"],
      "type": "string"
    },
    "name": {
      "description": "Name of the user",
      "examples": ["Joe"],
      "type": "string"
    },
    "role": {
      "description": "Default role assigned to the user",
      "additionalProperties": false,
      "properties": {
        "id": {
          "description": "ID of the role",
          "examples": ["1"],
          "type": "string"
        },
        "name": {
          "description": "Name of the role",
          "examples": ["user"],
          "type": "string"
        }
      },
      "required": ["id", "name"],
      "type": "object",
      "x-readme-ref-name": "UserRoleResponse"
    }
  },
  "required": ["id", "name"],
  "type": "object",
  "x-readme-ref-name": "CreateUserResponse"
}
