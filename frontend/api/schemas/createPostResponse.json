{
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "description": "A URL to the JSON Schema for this object.",
      "examples": ["https://example.com/schemas/CreatePostResponse.json"],
      "format": "uri",
      "readOnly": true,
      "type": "string"
    },
    "author": { "type": "string" },
    "college": { "type": "string" },
    "content": {
      "examples": [
        "My name is Bob Joe and I am a rising senior who just got into NEU. What is the fencing program like? Are they competitive?"
      ],
      "type": "string"
    },
    "id": { "type": "string" },
    "is_anonymous": { "type": "boolean" },
    "sport": { "type": "string" },
    "tag": {
      "items": {
        "additionalProperties": false,
        "properties": {
          "created_at": { "format": "date-time", "type": "string" },
          "deleted_at": { "format": "date-time", "type": ["string", "null"] },
          "id": { "type": "string" },
          "name": {
            "description": "The name of the tag",
            "examples": ["Hockey"],
            "type": "string"
          },
          "updated_at": { "format": "date-time", "type": "string" }
        },
        "required": ["id", "created_at", "updated_at", "deleted_at", "name"],
        "type": "object",
        "x-readme-ref-name": "Tag"
      },
      "type": ["array", "null"]
    },
    "title": {
      "examples": ["Looking for thoughts on NEU Fencing!"],
      "type": "string"
    }
  },
  "required": [
    "id",
    "author",
    "sport",
    "college",
    "tag",
    "title",
    "content",
    "is_anonymous"
  ],
  "type": "object",
  "x-readme-ref-name": "CreatePostResponse"
}
