{
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "description": "A URL to the JSON Schema for this object.",
      "examples": ["https://example.com/schemas/AssignRoleResponse.json"],
      "format": "uri",
      "readOnly": true,
      "type": "string"
    },
    "role": {
      "description": "Assigned role",
      "additionalProperties": false,
      "properties": {
        "id": {
          "description": "ID of the role",
          "examples": ["1"],
          "type": "string"
        },
        "name": {
          "description": "Name of the role",
          "examples": ["user"],
          "type": "string"
        }
      },
      "required": ["id", "name"],
      "type": "object",
      "x-readme-ref-name": "UserRoleResponse"
    },
    "user_id": {
      "description": "ID of the user",
      "examples": ["1"],
      "type": "string"
    }
  },
  "required": ["user_id", "role"],
  "type": "object",
  "x-readme-ref-name": "AssignRoleResponse"
}
