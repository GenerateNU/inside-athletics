{
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "description": "A URL to the JSON Schema for this object.",
      "examples": [
        "https://example.com/schemas/RegisterStripeCustomerResponse.json"
      ],
      "format": "uri",
      "readOnly": true,
      "type": "string"
    },
    "id": {
      "description": "ID of the user on stripe",
      "examples": ["1"],
      "type": "string"
    }
  },
  "required": ["id"],
  "type": "object",
  "x-readme-ref-name": "RegisterStripeCustomerResponse"
}
