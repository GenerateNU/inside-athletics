{
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "description": "A URL to the JSON Schema for this object.",
      "examples": ["https://example.com/schemas/UpdateTagBody.json"],
      "format": "uri",
      "readOnly": true,
      "type": "string"
    },
    "name": {
      "description": "The new name to update",
      "examples": ["Hockey"],
      "type": "string"
    }
  },
  "required": ["name"],
  "type": "object",
  "x-readme-ref-name": "UpdateTagBody"
}
