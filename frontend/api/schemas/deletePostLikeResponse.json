{
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "description": "A URL to the JSON Schema for this object.",
      "examples": ["https://example.com/schemas/DeletePostLikeResponse.json"],
      "format": "uri",
      "readOnly": true,
      "type": "string"
    },
    "liked": {
      "description": "Whether the requested user has liked the post",
      "examples": [true],
      "type": "boolean"
    },
    "message": {
      "description": "Message to display",
      "examples": ["Like was deleted successfully"],
      "type": "string"
    },
    "total": {
      "description": "Total number of likes on the post",
      "examples": [25],
      "format": "int64",
      "type": "integer"
    }
  },
  "required": ["message", "total", "liked"],
  "type": "object",
  "x-readme-ref-name": "DeletePostLikeResponse"
}
