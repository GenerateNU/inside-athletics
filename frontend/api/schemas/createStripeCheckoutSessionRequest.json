{
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "description": "A URL to the JSON Schema for this object.",
      "examples": [
        "https://example.com/schemas/CreateStripeCheckoutSessionRequest.json"
      ],
      "format": "uri",
      "readOnly": true,
      "type": "string"
    },
    "cancel_url": {
      "examples": ["https://example.com/cancel"],
      "type": "string"
    },
    "price_id": { "examples": ["price_12345"], "type": "string" },
    "quantity": { "examples": [1], "format": "int64", "type": "integer" },
    "success_url": {
      "examples": ["https://example.com/success"],
      "type": "string"
    }
  },
  "required": ["price_id", "success_url", "cancel_url", "quantity"],
  "type": "object",
  "x-readme-ref-name": "CreateStripeCheckoutSessionRequest"
}
