{
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "description": "A URL to the JSON Schema for this object.",
      "examples": [
        "https://example.com/schemas/GetCommentLikeInfoResponse.json"
      ],
      "format": "uri",
      "readOnly": true,
      "type": "string"
    },
    "liked": {
      "description": "Whether the requested user has liked the comment",
      "examples": [true],
      "type": "boolean"
    },
    "total": {
      "description": "Total number of likes on the comment",
      "examples": [25],
      "format": "int64",
      "type": "integer"
    }
  },
  "required": ["total", "liked"],
  "type": "object",
  "x-readme-ref-name": "GetCommentLikeInfoResponse"
}
