{
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "description": "A URL to the JSON Schema for this object.",
      "examples": ["https://example.com/schemas/DeleteContentResponse.json"],
      "format": "uri",
      "readOnly": true,
      "type": "string"
    },
    "message": { "description": "Success message", "type": "string" }
  },
  "required": ["message"],
  "type": "object",
  "x-readme-ref-name": "DeleteContentResponse"
}
