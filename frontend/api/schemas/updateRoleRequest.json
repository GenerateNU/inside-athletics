{
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "description": "A URL to the JSON Schema for this object.",
      "examples": ["https://example.com/schemas/UpdateRoleRequest.json"],
      "format": "uri",
      "readOnly": true,
      "type": "string"
    },
    "name": {
      "description": "Name of the role",
      "examples": ["moderator"],
      "type": "string"
    },
    "permissions": {
      "description": "Permissions to attach to the role",
      "items": {
        "additionalProperties": false,
        "properties": {
          "action": {
            "description": "Permission action",
            "examples": ["create"],
            "type": "string"
          },
          "resource": {
            "description": "Permission resource",
            "examples": ["sport"],
            "type": "string"
          }
        },
        "required": ["action", "resource"],
        "type": "object",
        "x-readme-ref-name": "PermissionSpecInput"
      },
      "type": "array"
    }
  },
  "type": "object",
  "x-readme-ref-name": "UpdateRoleRequest"
}
