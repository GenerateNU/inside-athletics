{
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "description": "A URL to the JSON Schema for this object.",
      "examples": ["https://example.com/schemas/CreateTagResponse.json"],
      "format": "uri",
      "readOnly": true,
      "type": "string"
    },
    "id": {
      "description": "ID of the tag created",
      "examples": ["1"],
      "type": "string"
    },
    "name": {
      "description": "The name of the tag created",
      "examples": ["Hockey"],
      "type": "string"
    },
    "type": {
      "description": "The type of the tag",
      "examples": ["sports"],
      "type": "string"
    }
  },
  "required": ["id", "name", "type"],
  "type": "object",
  "x-readme-ref-name": "CreateTagResponse"
}
