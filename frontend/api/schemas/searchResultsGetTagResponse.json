{
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "description": "A URL to the JSON Schema for this object.",
      "examples": [
        "https://example.com/schemas/SearchResultsGetTagResponse.json"
      ],
      "format": "uri",
      "readOnly": true,
      "type": "string"
    },
    "results": {
      "items": {
        "additionalProperties": false,
        "properties": {
          "$schema": {
            "description": "A URL to the JSON Schema for this object.",
            "examples": ["https://example.com/schemas/GetTagResponse.json"],
            "format": "uri",
            "readOnly": true,
            "type": "string"
          },
          "id": {
            "description": "ID of the tag",
            "examples": ["1"],
            "type": "string"
          },
          "name": {
            "description": "The name of the tag",
            "examples": ["Hockey"],
            "type": "string"
          }
        },
        "required": ["id", "name"],
        "type": "object",
        "x-readme-ref-name": "GetTagResponse"
      },
      "type": ["array", "null"]
    }
  },
  "required": ["results"],
  "type": "object",
  "x-readme-ref-name": "SearchResultsGetTagResponse"
}
