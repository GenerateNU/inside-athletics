{
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "description": "A URL to the JSON Schema for this object.",
      "examples": ["https://example.com/schemas/AssignRoleRequest.json"],
      "format": "uri",
      "readOnly": true,
      "type": "string"
    },
    "role_id": {
      "description": "ID of the role to assign",
      "examples": ["1"],
      "type": "string"
    }
  },
  "required": ["role_id"],
  "type": "object",
  "x-readme-ref-name": "AssignRoleRequest"
}
