{
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "description": "A URL to the JSON Schema for this object.",
      "examples": ["https://example.com/schemas/GetDownloadURLResponse.json"],
      "format": "uri",
      "readOnly": true,
      "type": "string"
    },
    "download_url": { "type": "string" },
    "expires_in": { "format": "int64", "type": "integer" }
  },
  "required": ["download_url", "expires_in"],
  "type": "object",
  "x-readme-ref-name": "GetDownloadURLResponse"
}
