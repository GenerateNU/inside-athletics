{
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "description": "A URL to the JSON Schema for this object.",
      "examples": ["https://example.com/schemas/AccessCheckResponse.json"],
      "format": "uri",
      "readOnly": true,
      "type": "string"
    },
    "has_premium": {
      "description": "Whether the user has premium content access",
      "type": "boolean"
    },
    "is_admin": {
      "description": "Whether the user has admin access",
      "type": "boolean"
    }
  },
  "required": ["has_premium", "is_admin"],
  "type": "object",
  "x-readme-ref-name": "AccessCheckResponse"
}
