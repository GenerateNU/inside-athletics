{
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "description": "A URL to the JSON Schema for this object.",
      "examples": ["https://example.com/schemas/CreatePostRequest.json"],
      "format": "uri",
      "readOnly": true,
      "type": "string"
    },
    "college_id": { "type": "string" },
    "content": {
      "examples": [
        "My name is Bob Joe and I am a rising senior who just got into NEU. What is the fencing program like? Are they competitive?"
      ],
      "maxLength": 5000,
      "minLength": 1,
      "type": "string"
    },
    "is_anonymous": { "type": "boolean" },
    "sport_id": { "type": "string" },
    "tags": {
      "items": {
        "additionalProperties": false,
        "properties": { "id": { "type": "string" } },
        "required": ["id"],
        "type": "object",
        "x-readme-ref-name": "TagRequest"
      },
      "type": ["array", "null"]
    },
    "title": {
      "examples": ["Looking for thoughts on NEU Fencing!"],
      "maxLength": 100,
      "minLength": 1,
      "type": "string"
    }
  },
  "required": ["title", "content", "is_anonymous"],
  "type": "object",
  "x-readme-ref-name": "CreatePostRequest"
}
