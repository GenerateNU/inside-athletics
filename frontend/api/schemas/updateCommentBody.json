{
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "description": "A URL to the JSON Schema for this object.",
      "examples": ["https://example.com/schemas/UpdateCommentBody.json"],
      "format": "uri",
      "readOnly": true,
      "type": "string"
    },
    "description": {
      "description": "Updated comment text",
      "examples": ["This is a helpful thread"],
      "maxLength": 300,
      "type": "string"
    }
  },
  "required": ["description"],
  "type": "object",
  "x-readme-ref-name": "UpdateCommentBody"
}
