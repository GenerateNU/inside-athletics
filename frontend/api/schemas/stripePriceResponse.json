{
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "description": "A URL to the JSON Schema for this object.",
      "examples": ["https://example.com/schemas/StripePriceResponse.json"],
      "format": "uri",
      "readOnly": true,
      "type": "string"
    },
    "active": { "examples": [true], "type": "boolean" },
    "created": {
      "examples": [1700000000],
      "format": "int64",
      "type": "integer"
    },
    "currency": { "examples": ["usd"], "type": "string" },
    "id": { "examples": ["price_12345"], "type": "string" },
    "interval": { "examples": ["month"], "type": "string" },
    "interval_count": { "examples": [1], "format": "int64", "type": "integer" },
    "product_id": { "examples": ["prod_12345"], "type": "string" },
    "unit_amount": { "examples": [2000], "format": "int64", "type": "integer" }
  },
  "required": [
    "id",
    "product_id",
    "unit_amount",
    "currency",
    "interval",
    "interval_count",
    "active",
    "created"
  ],
  "type": "object",
  "x-readme-ref-name": "StripePriceResponse"
}
