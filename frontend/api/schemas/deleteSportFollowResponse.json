{
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "description": "A URL to the JSON Schema for this object.",
      "examples": [
        "https://example.com/schemas/DeleteSportFollowResponse.json"
      ],
      "format": "uri",
      "readOnly": true,
      "type": "string"
    },
    "message": {
      "description": "Message to display",
      "examples": ["Sport follow was deleted successfully"],
      "type": "string"
    }
  },
  "required": ["message"],
  "type": "object",
  "x-readme-ref-name": "DeleteSportFollowResponse"
}
